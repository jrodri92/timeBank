<div class="container p-4">
  <div class='row'>
    <div class='ccol-md-4 mx-auto'>
      <div class="card" >
        <div class="card-body">
          <form action="/ofertas/misofertas/edit/{{oferta.id_oferta}}" method="POST">
            <div class="form-group">
              <input type='text' value="{{oferta.nombre_oferta}}" class="form-control" name="nombre_oferta" placeholder="Nombre" autofocus>
            </div>
            <!--
            <div class="form-group">
              <input type="url" value="{{link.url}}" name="url" class = "form-control" placeholder="URL">
            </div>
            -->
            <div class="form-group">
              <textarea name="descripcion" rows="2" class="form-control" placeholder="Descripcion">{{oferta.oferta_descripcion}}</textarea>
            </div>
            <div class="form-group" >
              <button class="btn btn-success btn-block">
                save
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>