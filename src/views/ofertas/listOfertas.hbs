<!-- Vista para ver todas las ofertas, aqui puede solicitar o ver mas -->

<div class="container p-4">
  <div class="row">
    {{#each ofertas}}
      <div class="col-md-3">
        <div class="card text-center">
          <div class="card-body">
              <a href="{{url}}" target="_blank">
                <h3 class="card-title text-uppercase">
                  {{nombre_oferta}}
                </h3>
              </a>
              <p class="m-2">{{descripcion}}</p>
              <p>{{timeago fechaCreacion}}</p>
              <a href="/ofertas/perfilofertante/{{id_usuario}}" class="btn btn-danger">Ver más</a>
              <a href="/ofertas/edit/{{id}}" class="btn btn-secondary">Solicitar</a>
          </div>
        </div>
      </div>
    {{else}}
      <div class="col-md-4 mx-auto">
        <div class="card card-body text-center">
          <p> No hay links aun </p>
          <a href="/ofertas/add"> Crea uno! </a>
        </div>
      </div>
    {{/each}}
  </div>
</div>